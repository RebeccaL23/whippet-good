<div class="container">
  <h1>Leave a review</h1>
  <%= simple_form_for [@dog, @review] do |f| %>
    <%= f.input :rating, collection: 1..5 %>
    <%= f.input :content %>
    <%= f.button :submit %>
  <% end %>
</div>
